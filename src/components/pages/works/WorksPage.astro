---
import PageBuilder from "@/components/pages/builder/PageBuilder.astro";
import type { WorksPageProps } from "@/types/components/pages/works/WorksPage.types";

const { data } = Astro.props as WorksPageProps;
---

<PageBuilder page={data.page} context={data.context} />
