---
import { pCardTemplates } from "@/constants/components/object/project/card/PCard.constants";
import type { PCardProps } from "@/types/components/object/project/card/PCard.types";

const {
	template = "grid",
	...props
} = Astro.props as PCardProps;

const Template = pCardTemplates[template];
---

<Template {...props} template={template} />
