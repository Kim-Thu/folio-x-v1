---
import CCard from "@/components/object/component/card/CCard.astro";
import CColumns from "@/components/object/component/CColumns.astro";
import CGridItem from "@/components/object/component/CGridItem.astro";
import type { PCardTemplateProps } from "@/types/components/object/project/card/PCard.types";

const {
	gap = "lg",
	card,
	items,
	...attributes
} = Astro.props as PCardTemplateProps;

const featured = items[0];
const supporting = items.slice(1, 4);
const featuredCard = card ?? {};
---

<CColumns
	{...attributes}
	columns="twelve"
	gap={gap}
	align="start"
	data-card-collection
	data-view="editorial-split"
>
	{featured && (
		<CGridItem lgSpan={7} align="start">
			<CCard {...featuredCard} data={featured} index={0} />
		</CGridItem>
	)}

	<CGridItem lgSpan={5} align="start">
		<CColumns columns="one" gap="lg" separator="light">
			{supporting.map((item, index) => (
				<CCard
					template="editorial-list"
					data={item}
					index={index + 1}
					headingLevel={featuredCard.headingLevel}
					slots={{
						media: false,
						icon: false,
						metadata: true,
						tags: false,
						metrics: false,
						title: true,
						excerpt: true,
						action: false,
					}}
				/>
			))}
		</CColumns>
	</CGridItem>
</CColumns>
