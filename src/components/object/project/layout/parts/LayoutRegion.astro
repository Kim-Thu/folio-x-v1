---
import LContainer from "@/components/layout/LContainer.astro";
import LSection from "@/components/layout/LSection.astro";
import LayoutRegionContent from "@/components/object/project/layout/parts/LayoutRegionContent.astro";
import type { PageRegionProps } from "@/types/components/object/project/layout/PLayout.types";

const { region } = Astro.props as PageRegionProps;
const section = region.section;
const {
	id,
	theme = "none",
	spacing = "none",
	container = "none",
} = section || {};
---

{section === false ? (
	<LayoutRegionContent {region} />
) : (
	<LSection {id} {theme} {spacing} data-page-region={region.key}>
		{container === "none" ? (
			<LayoutRegionContent {region} />
		) : (
			<LContainer size={container}>
				<LayoutRegionContent {region} />
			</LContainer>
		)}
	</LSection>
)}
