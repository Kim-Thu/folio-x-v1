---
import type { CardsProps } from '@/types/ui';

type Props = CardsProps;

const { class: className = '', ...attributes } = Astro.props;
---

<div {...attributes} class={className}>
  <slot />
</div>
