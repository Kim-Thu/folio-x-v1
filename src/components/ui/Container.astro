---
import type { ContainerProps } from '../../types/ui';

type Props = ContainerProps;

const { class: className = '' } = Astro.props;
---

<div class:list={['container-shell', className]}>
  <slot />
</div>
