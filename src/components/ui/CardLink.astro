---
import type { CardLinkProps } from '../../types/ui';

type Props = CardLinkProps;

const { href, class: className = '', ariaLabel } = Astro.props;
---

<a href={href} aria-label={ariaLabel} class={className}>
  <slot />
</a>
