---
import type { PrincipleTitleProps } from '../../types/ui';

type Props = PrincipleTitleProps;

const { class: className = '' } = Astro.props;
---

<h3 class:list={['text-h3 font-semibold', className]}>
  <slot />
</h3>
