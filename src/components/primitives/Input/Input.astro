---
import type { InputProps } from '@/types/ui';
import { getInputClasses } from '@/components/primitives/Input/variants/getInputClasses';

type Props = InputProps;

const { class: className = '', variant = 'default', ...attributes } = Astro.props;
---

<input
  {...attributes}
  class={getInputClasses(variant, className)}
/>
