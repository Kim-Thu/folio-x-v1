---
import type { ProjectCardProps } from '@/types/ui';
import TileProjectCard from '@/components/features/projects/ProjectCard/variants/TileProjectCard.astro';
import ArchiveProjectCard from '@/components/features/projects/ProjectCard/variants/ArchiveProjectCard.astro';

type Props = ProjectCardProps;

const props = Astro.props;
---

{props.variant === 'archive' ? <ArchiveProjectCard {...props} /> : <TileProjectCard {...props} />}
