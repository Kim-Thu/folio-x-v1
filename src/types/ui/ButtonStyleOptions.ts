import type { ButtonSize, ButtonTone, ButtonVariant } from '@/types/ui/ButtonCommonProps';

export interface ButtonStyleOptions {
  iconOnly?: boolean;
  size: ButtonSize;
  tone: ButtonTone;
  variant: ButtonVariant;
}
